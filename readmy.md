Алго активации проекта на новом месте:
## Создать и активировать виртуальное окружение (в новом месте)
В терминале:

python -m venv venv

venv\Scripts\activate  # Для Windows
## Установить зависимости
pip install -r requirements.txt

## Запустить проект